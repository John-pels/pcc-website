const logo = require("./logo.svg")
const logoLg = require("./logo-lg.svg")
const not_found = require("./404.svg")
const waveBg = require("./wave-bg.svg")

const images = {
  logo,
  logoLg,
  not_found,
  waveBg,
}

export default images
