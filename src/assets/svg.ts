const not_found = require("./404.svg")
const waveBg = require("./wave-bg.svg")
const Contact = require("./contact.svg")
const BrandLogo = require("./pcc-brand.svg")

const images = {
  BrandLogo,
  not_found,
  waveBg,
  Contact,
}

export default images
